export const fetchCounter = () => {
    const options = {
      method: "GET",
      headers: {

      },
    };
    return async (dispatch) => {

      dispatch({
        type: "STORAGE_USERS",
        payload: json,
      });
    };
  };