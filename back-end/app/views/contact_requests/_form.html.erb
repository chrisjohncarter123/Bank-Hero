<%= form_with(model: contact_request, local: true) do |form| %>
  <% if contact_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_request.errors.count, "error") %> prohibited this contact_request from being saved:</h2>

      <ul>
        <% contact_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
